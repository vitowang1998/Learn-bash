git

git branch
-prints all the local branches

git status
-prints the current git status

git clean -n
-prints the files that would be removed

git clean -f
-remove the untracked files

git clean -d -f
-remove all untracked files and directories

git stash
-stores the current tracked work in a dirty directory

git stash apply
-restores the stashed work from the dirty directory
